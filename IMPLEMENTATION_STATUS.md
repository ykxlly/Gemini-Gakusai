# 実装状況と次フェーズ（2026-09-21）

## 現在地

このリポジトリは、学園祭向け AI おみくじの MVP です。Next.js 15 / React 19 / TypeScript で、画面と Gemini API を呼ぶ 7 つの API ルートが実装されています。直近のコミットは `a627dac`（Prepare Vercel deployment）です。

| 領域 | 実装状況 |
| --- | --- |
| おみくじ | 気分・目的・同行者・任意の MBTI と同行者の気分を受け、運勢、ミッション、ラッキー要素を生成 |
| ミッション | 手動達成、なぞなぞ判定、写真判定 |
| 追加体験 | お守りカードの文言・色、音声読み上げ、会話、複数回の結果のまとめ |
| データ | `src/data/spots.json` に 5 件のスポットを固定登録 |
| 保存 | 運勢名とメッセージをブラウザーの localStorage に最大 10 件保存。サーバー側の DB やユーザー認証はなし |
| 配布状況 | `huhefu/gemini-gakusai` を作成し、[本番 URL](https://gemini-gakusai.vercel.app/) にデプロイ済み。Production に `GEMINI_API_KEY` を設定し、主要 API の成功応答を確認済み |

## 確認結果

- `npm ci`：成功。
- `npm run lint`：初回は終了コード 0、設定ファイルの警告が 2 件。ビルド後は生成された `.next` まで検査して失敗したため、生成物を除外する設定と警告箇所を修正した。
- `npm run build`：成功。トップページと 7 つの API ルートを生成。
- `npm audit`：2 件（moderate 1、high 1）。Next.js が参照する PostCSS の脆弱性。Next.js のメジャー更新が修正候補として提示されるため、互換性確認を伴う更新が必要。
- 本番 API：おみくじ、カード、なぞなぞ、会話、まとめ、音声、写真判定の 7 ルートが HTTP 200 を返すことを確認。写真判定は同梱画像を使った疎通確認で、実際の来場者写真による判定品質は未評価。
- Vercel：CLI で `huhefu/gemini-gakusai` を作成。最初のデプロイは Framework Preset が `Other` となり 404 だったため、`Next.js` に変更して再デプロイした。GitHub リポジトリを接続済み。
- Gemini：当初 `gemini-3.6-flash` が `503 UNAVAILABLE` を返したため、一時的な 503 の際は短く待って `gemini-3.5-flash` に切り替える処理を追加。修正後に本番のおみくじ生成が成功した。

## 公開前に解消したい点

1. **費用と濫用対策**：7 つの API は認証・レート制限なしで Gemini を呼べる。特にチャット、画像判定、音声生成の連続実行が可能。
2. **入力の上限**：文字列の長さ、チャット履歴の件数、写真のサイズを制限していない。大きなリクエストは費用、応答時間、サーバー負荷に影響する。
3. **モデル応答の検証**：JSON をパースしてそのまま返している。構造やスポット名が期待どおりかサーバー側で検証していない。
4. **実データ**：スポットはサンプルの 5 件。実際の学園祭の出店、場所、開催日時、営業状況との照合が必要。
5. **プライバシー説明**：画面上の文面は、入力と写真が AI に送信されることを示す表現に修正した。公開時は Gemini 側のデータ処理も確認し、正式な説明を用意する必要がある。
6. **運用**：基本的な API キー設定・確認手順を README に追加した。障害時の案内、ログと費用の監視、詳細なリリース確認手順はまだない。

## 次フェーズ案：デプロイ済み環境の安定化

1. API 入力の長さ・形式・画像サイズを検証し、エラー応答を統一する。
2. API の利用回数を制御する仕組みを導入し、Gemini 利用量を監視する。
3. モデルの返却値を検証し、異常応答でも画面が壊れないようにする。
4. Next.js と依存関係の更新を別作業で検証し、監査結果を解消する。
5. 実スポットデータと環境変数を設定し、Gemini を使った一連の操作を実機で確認する。

公開可否の目安は、実データが揃い、主要導線をスマートフォンで通せて、利用上限と監視が機能し、既知の高リスク脆弱性が解消された状態です。
